# Trading Bot 代码详解

## 📋 目录
1. [项目概述](#项目概述)
2. [核心类与数据结构](#核心类与数据结构)
3. [主要功能模块](#主要功能模块)
4. [交易流程详解](#交易流程详解)
5. [关键参数说明](#关键参数说明)

---

## 项目概述

这是一个支持多交易所的自动化交易机器人，主要用于**刷交易量**策略。核心思路是：
- 在市场价附近下限价开仓单
- 订单成交后立即下止盈平仓单
- 循环往复，积累交易量
- 期望价格回归时实现零磨损

**支持的交易所**：EdgeX, Backpack, Paradex, Aster, Lighter

---

## 核心类与数据结构

### 1. TradingConfig（交易配置类）

存储所有交易参数的配置对象：

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `ticker` | str | 交易标的符号 | "ETH", "BTC" |
| `contract_id` | str | 合约ID（自动获取） | 由交易所API返回 |
| `quantity` | Decimal | 每笔订单数量 | 0.1 |
| `take_profit` | Decimal | 止盈百分比 | 0.02（表示0.02%） |
| `tick_size` | Decimal | 最小价格变动 | 由交易所返回 |
| `direction` | str | 交易方向 | "buy"（做多）/ "sell"（做空） |
| `max_orders` | int | 最大活跃订单数 | 40 |
| `wait_time` | int | 订单间等待时间（秒） | 450 |
| `exchange` | str | 交易所名称 | "edgex", "backpack"等 |
| `grid_step` | Decimal | 网格步长（%） | 0.5（-100表示无限制） |
| `stop_price` | Decimal | 停止交易价格 | 5500（-1表示不设置） |
| `pause_price` | Decimal | 暂停交易价格 | 5000（-1表示不设置） |
| `aster_boost` | bool | Aster Boost模式 | True/False |

**关键属性方法**：
- `close_order_side`: 根据交易方向自动返回平仓方向
  - 做多(buy) → 平仓方向是卖出(sell)
  - 做空(sell) → 平仓方向是买入(buy)

### 2. OrderMonitor（订单监控类）

用于跟踪订单状态的数据类：

```python
@dataclass
class OrderMonitor:
    order_id: Optional[str] = None      # 订单ID
    filled: bool = False                # 是否已成交
    filled_price: Optional[Decimal]     # 成交价格
    filled_qty: Decimal = 0.0           # 成交数量
```

### 3. TradingBot（交易机器人核心类）

机器人的主要逻辑实现类。

**核心状态变量**：
- `active_close_orders`: 当前所有活跃的平仓订单列表
- `last_close_orders`: 上次检查的订单数量（用于判断订单是否减少）
- `last_open_order_time`: 上次下单时间戳（用于冷却时间计算）
- `order_filled_event`: 异步事件，订单成交时触发
- `order_canceled_event`: 异步事件，订单取消时触发
- `shutdown_requested`: 是否请求关闭机器人

---

## 主要功能模块

### 1. 初始化模块（`__init__`）

**功能**：
1. 创建日志记录器
2. 根据配置创建对应交易所的客户端
3. 初始化所有状态变量
4. 注册WebSocket订单更新处理器

**关键代码**：
```python
self.exchange_client = ExchangeFactory.create_exchange(
    config.exchange,
    config
)
self._setup_websocket_handlers()
```

### 2. WebSocket订单处理（`_setup_websocket_handlers`）

**功能**：
实时监听交易所返回的订单更新，处理不同状态的订单。

**订单状态处理**：
- `FILLED`（完全成交）
  - 开仓订单成交 → 触发`order_filled_event`事件
  - 记录成交信息到日志
  
- `CANCELED`（已取消）
  - 记录部分成交数量
  - 触发`order_canceled_event`事件
  
- `PARTIALLY_FILLED`（部分成交）
  - 记录部分成交信息

**线程安全机制**：
```python
# WebSocket回调可能在不同线程，需要线程安全地触发事件
if self.loop is not None:
    self.loop.call_soon_threadsafe(self.order_filled_event.set)
```

### 3. 等待时间计算（`_calculate_wait_time`）

**功能**：
动态计算订单之间的等待时间，实现智能下单频率控制。

**策略逻辑**：
```
订单数量占比          冷却时间
---------------------------------
>= 2/3 max_orders → 2 × wait_time (保守)
>= 1/3 max_orders → 1 × wait_time (正常)
>= 1/6 max_orders → 0.5 × wait_time (激进)
< 1/6 max_orders  → 0.25 × wait_time (最激进)
订单数减少时      → 立即下单（0秒）
达到max_orders   → 等待（1秒后重检）
```

**返回值**：
- `0`: 可以立即下单
- `1`: 需要等待1秒后重新检查

### 4. 网格步长检查（`_meet_grid_step_condition`）

**功能**：
检查新订单的平仓价格与现有平仓订单的价格间距是否满足`grid_step`要求。

**做多(buy)逻辑**：
```python
# 选择价格最低的现有平仓订单
next_close_order = min(active_close_orders, key=lambda o: o["price"])

# 计算新订单的平仓价格
new_order_close_price = best_ask * (1 + take_profit/100)

# 检查：现有平仓价 / 新平仓价 > 1 + grid_step%
# 即：新平仓价必须比现有最低平仓价低至少grid_step%
if next_close_price / new_order_close_price > 1 + grid_step/100:
    return True  # 满足条件，可以下单
```

**做空(sell)逻辑**：
```python
# 选择价格最高的现有平仓订单
next_close_order = max(active_close_orders, key=lambda o: o["price"])

# 计算新订单的平仓价格
new_order_close_price = best_bid * (1 - take_profit/100)

# 检查：新平仓价 / 现有平仓价 > 1 + grid_step%
# 即：新平仓价必须比现有最高平仓价高至少grid_step%
if new_order_close_price / next_close_price > 1 + grid_step/100:
    return True  # 满足条件，可以下单
```

**示例**：
```
做多，grid_step=0.5%，现有平仓订单价格=2000 USDT
→ 新平仓价必须 ≤ 1990 USDT (2000 × 0.995)
→ 如果当前计算的新平仓价是2000.8，则不满足条件，等待
```

### 5. 价格条件检查（`_check_price_condition`）

**功能**：
检查市场价格是否触发停止或暂停条件。

**做多(buy)方向**：
```python
if direction == "buy":
    if price >= stop_price:   # 价格涨太高，停止交易退出
        stop_trading = True
    if price >= pause_price:  # 价格涨太高，暂停等待回落
        pause_trading = True
```

**做空(sell)方向**：
```python
if direction == "sell":
    if price <= stop_price:   # 价格跌太低，停止交易退出
        stop_trading = True
    if price <= pause_price:  # 价格跌太低，暂停等待回升
        pause_trading = True
```

**使用场景**：
- `stop_price`: 设置价格上限/下限，防止在不利价格继续交易
- `pause_price`: 临时暂停，等待价格回归合理区间

### 6. 下单与监控（`_place_and_monitor_open_order`）

**功能**：
下开仓订单并监控其执行情况。

**流程**：
```
1. 重置状态
   ├─ 清除成交事件
   ├─ 设置订单状态为OPEN
   └─ 重置成交数量

2. 下开仓订单
   └─ 调用交易所API下单

3. 等待成交（最多10秒）
   ├─ 如果立即成交 → 直接处理
   └─ 否则等待order_filled_event触发

4. 处理订单结果
   └─ 调用_handle_order_result
```

### 7. 订单结果处理（`_handle_order_result`）

**功能**：
根据订单执行结果采取相应行动。

**情况1：订单完全成交**
```python
if order_filled_event.is_set() or status == 'FILLED':
    # 计算平仓价格
    if close_side == 'sell':
        close_price = filled_price * (1 + take_profit/100)  # 做多：更高价卖出
    else:
        close_price = filled_price * (1 - take_profit/100)  # 做空：更低价买入
    
    # 下平仓订单
    place_close_order(quantity, close_price, close_side)
```

**情况2：订单未成交（超时）**
```python
else:
    # 取消订单
    cancel_order(order_id)
    
    # 等待取消确认
    await order_canceled_event.wait(timeout=5)
    
    # 如果有部分成交...（见情况3）
```

**情况3：订单部分成交**
```python
if order_filled_amount > 0:
    # 为成交部分下平仓订单
    place_close_order(
        order_filled_amount,  # 使用实际成交数量
        close_price,
        close_side
    )
```

**Aster Boost模式**：
```python
if aster_boost:
    # 使用市价单立即平仓（速度快但磨损高）
    place_market_order(quantity, close_side)
else:
    # 使用限价单平仓（磨损低但可能不成交）
    place_close_order(quantity, close_price, close_side)
```

### 8. 状态监控（`_log_status_periodically`）

**功能**：
每60秒记录一次状态，并检查仓位健康度。

**检查项**：
1. 获取所有活跃订单
2. 筛选出平仓订单
3. 获取当前持仓数量
4. 计算平仓订单总数量
5. **关键检查**：`|持仓数量 - 平仓订单总量| > 2×quantity`

**仓位不匹配处理**：
```python
if abs(position_amt - active_close_amount) > (2 * quantity):
    # 这是严重问题！
    # 1. 记录详细错误信息
    # 2. 发送飞书通知（如果配置了）
    # 3. 请求关闭机器人
    shutdown_requested = True
```

**为什么检查仓位匹配？**
- 理论上：每个持仓都应该有对应的平仓订单
- 如果不匹配：可能存在未对冲的风险敞口
- 需要手动介入检查和修复

### 9. 主运行循环（`run`）

**功能**：
机器人的核心事件循环，协调所有功能模块。

**完整流程图**：
```
START
  ↓
1. 获取合约信息（contract_id, tick_size）
  ↓
2. 记录配置信息
  ↓
3. 连接交易所（WebSocket等）
  ↓
4. 等待5秒建立连接
  ↓
5. 主循环 ←─────────────────┐
  ↓                         │
6. 获取活跃订单             │
  ↓                         │
7. 筛选平仓订单             │
  ↓                         │
8. 定期记录状态（60秒）     │
  ↓                         │
9. 检查价格条件             │
  ├─ 停止？→ 优雅关闭 → END │
  ├─ 暂停？→ 等待5秒 ────┘  │
  ↓                         │
10. 检查仓位不匹配          │
  └─ 有问题？→ 等待 ──────┘  │
  ↓                         │
11. 计算等待时间            │
  └─ 需等待？→ 等待 ──────┘  │
  ↓                         │
12. 检查网格步长            │
  └─ 不满足？→ 等待1秒 ───┘  │
  ↓                         │
13. 下单并监控              │
  ↓                         │
14. 继续循环 ───────────────┘
```

**异常处理**：
- `KeyboardInterrupt`: 用户Ctrl+C → 优雅关闭
- `其他异常`: 记录错误 → 优雅关闭 → 抛出异常
- `finally`: 确保断开交易所连接

---

## 交易流程详解

### 完整交易周期示例

**假设条件**：
- 交易标的：ETH
- 当前价格：$2000
- 方向：做多(buy)
- 止盈：0.02%
- 订单数量：0.1 ETH

**步骤1：下开仓订单**

```
市场价：$2000
开仓价：$2000.40（略高于市价，确保作为maker）
订单：买入 0.1 ETH @ $2000.40
```

**步骤2：等待成交**

```
- WebSocket监听订单状态
- 最多等待10秒
- 如果成交 → 触发order_filled_event
- 如果超时 → 取消订单
```

**步骤3：订单成交**
```
实际成交价：$2000.40
成交数量：0.1 ETH
WebSocket推送：status=FILLED
```

**步骤4：计算平仓价**
```
做多平仓方向：卖出(sell)
平仓价 = $2000.40 × (1 + 0.02/100)
平仓价 = $2000.40 × 1.0002
平仓价 = $2000.80
```

**步骤5：下平仓订单**
```
订单：卖出 0.1 ETH @ $2000.80
订单类型：限价单（post-only）
等待市场价格上涨到$2000.80成交
```

**步骤6：平仓成交**
```
当市场价格涨到$2000.80
平仓订单成交
盈利 = ($2000.80 - $2000.40) × 0.1 = $0.04
```

**步骤7：继续循环**
```
- 活跃平仓订单数减少1
- 立即下新的开仓订单（wait_time=0）
- 重复整个流程
```

### 网格步长控制示例

**场景**：grid_step = 0.5%，做多

**初始状态**：
```
无活跃平仓订单
→ 可以下单
→ 下单后平仓价：$2000.80
```

**第二笔订单**：
```
现有平仓订单：$2000.80
当前市场价：$2000
新订单平仓价：$2000.80

检查：$2000.80 / $2000.80 = 1.0
需要：> 1.005（1 + 0.5%）
结果：1.0 < 1.005 → 不满足
→ 等待价格下跌或现有订单成交
```

**价格下跌后**：
```
现有平仓订单：$2000.80
当前市场价：$1990
新订单平仓价：$1990.40

检查：$2000.80 / $1990.40 ≈ 1.0052
需要：> 1.005
结果：1.0052 > 1.005 → 满足！
→ 可以下单
```

---

## 关键参数说明

### 1. quantity（订单数量）

**作用**：每笔订单的交易数量

**建议**：

- 小数量（0.01-0.1）：低风险，适合长期运行
- 大数量（0.5-1.0）：高风险，快速积累交易量

**注意**：
- 数量越小，被套的资金越少
- 数量越大，交易量增长越快

### 2. take_profit（止盈百分比）

**作用**：平仓订单的止盈幅度

**示例**：
- 0.01% → 非常小的利润，容易成交
- 0.02% → 平衡值（推荐）
- 0.05% → 更高利润，但可能难成交

**计算**：
```
做多：sell_price = buy_price × (1 + 0.02/100)
做空：buy_price = sell_price × (1 - 0.02/100)
```

### 3. max_orders（最大订单数）

**作用**：风险控制的核心参数

**影响**：
- 数量越多 → 潜在被套资金越大
- 数量越少 → 风险越小，但交易量增长慢

**建议**：
- 保守：10-20
- 平衡：30-40
- 激进：50+

### 4. wait_time（等待时间）

**作用**：订单间的基础冷却时间

**动态调整**：
```
订单数量 < 1/6 max → wait_time / 4
订单数量 < 1/3 max → wait_time / 2  
订单数量 < 2/3 max → wait_time
订单数量 >= 2/3 max → wait_time × 2
```

**建议**：
- 短期冲量：60-300秒
- 长期稳定：450-650秒（推荐）
- 超保守：900秒+

### 5. grid_step（网格步长）

**作用**：控制平仓订单的最小价格间距

**设置**：
- -100：无限制（最激进）
- 0.3-0.5%：平衡（推荐）
- 1.0%+：保守

**效果**：
```
grid_step = 0.5%

平仓订单1：$2000
平仓订单2：必须 ≤ $1990（做多）或 ≥ $2010（做空）

避免订单过于密集，提高成交概率
```

### 6. stop_price & pause_price

**stop_price（停止价格）**：
- 触发后 → 优雅关闭机器人
- 用于设置不可接受的价格上限/下限

**pause_price（暂停价格）**：
- 触发后 → 暂停下单，等待价格回归
- 用于临时避开不利价格区间

**示例**：
```
做多ETH，当前价格$2000
stop_price = 5500   → 价格到$5500停止（避免高位被套）
pause_price = 3000  → 价格到$3000暂停（等待回落）
```

### 7. aster_boost（Aster Boost模式）

**普通模式**：
```
开仓：限价单（maker）
平仓：限价单（maker）
磨损：2×maker手续费 + 可能的滑点
优点：磨损极低
缺点：平仓可能不成交
```

**Boost模式**：
```
开仓：限价单（maker）
平仓：市价单（taker）
磨损：1×maker + 1×taker手续费 + 滑点
优点：平仓速度快，100%成交
缺点：磨损较高
```

**适用场景**：
- 普通模式：长期稳定运行
- Boost模式：短期快速冲量

---

## 风险提示

⚠️ **重要警告**：

1. **无止损机制**
   - 策略不包含止损功能
   - 价格单向波动会持续被套
   - 必须有足够资金承受回撤

2. **仓位不匹配风险**
   - 如果检测到仓位不匹配，立即停机
   - 手动检查所有订单和持仓
   - 确保每个持仓都有对应平仓订单

3. **网络与API风险**
   - WebSocket断线可能丢失订单更新
   - API限流可能导致下单失败
   - 定期检查机器人运行状态

4. **市场风险**
   - 大幅波动可能导致大量被套
   - 流动性不足可能影响成交
   - 异常行情可能触发交易所风控

---

## 总结

这个交易机器人的核心思想是：

**通过频繁的小额交易积累交易量，期望在价格回归时实现零磨损**

关键要素：
1. ✅ 动态等待时间控制
2. ✅ 网格步长防止订单密集
3. ✅ 仓位匹配监控
4. ✅ 价格条件控制
5. ✅ 多交易所支持
6. ✅ 完善的日志和通知

建议的参数组合：
```bash
# 稳健型
--quantity 0.1 --take-profit 0.02 --max-orders 30 --wait-time 600 --grid-step 0.5

# 平衡型（推荐）
--quantity 0.1 --take-profit 0.02 --max-orders 40 --wait-time 450 --grid-step 0.3

# 激进型
--quantity 0.2 --take-profit 0.01 --max-orders 60 --wait-time 200 --grid-step -100
```

记住：**理解代码逻辑，合理设置参数，严格控制风险！**

